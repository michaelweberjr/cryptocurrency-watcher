import React, { useState, useEffect } from 'react';

const Signup = (props) => {
    const [state, setState] = useState(0);

    return (
        <h1>No signup page yet</h1>
    );
}

export default Signup;